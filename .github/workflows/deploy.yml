name: Deploy Vite React App to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install dependencies
        run: npm install

      - name: Copy images to public directory
        run: mkdir -p public/images && cp -R src/assets/images/* public/images/

      - name: List files in src/assets/images (source directory)
        run: ls -la src/assets/images

      - name: List files in public/images (after copying)
        run: ls -la public/images

      - name: Generate image descriptions
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: node generate_description.js

      - name: Build Vite project
        run: npm run build

      - name: Copy images to dist directory after build
        run: mkdir -p dist/images && cp -R public/images/* dist/images/

      - name: List files in dist directory after build
        run: ls -la dist/images

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
